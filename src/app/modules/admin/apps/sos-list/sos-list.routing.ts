import { Route } from '@angular/router';

import { SosListComponent } from './sos-list.component';

export const SosListRoutes: Route[] = [
    {
        path     : '',
        component: SosListComponent,
    }
];
